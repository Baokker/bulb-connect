# 连灯泡游戏 Specification

## 1. 目标与范围

实现一个单页 HTML 游戏：玩家旋转 5×5 棋盘中的线材和灯泡，使电源能够向所有灯泡供电。所有灯泡点亮时立即显示胜利状态。

首版仅包含以下能力：

- 固定 5×5 棋盘，且每局固定 5 个灯泡。
- 每局一个不可旋转的电源格。
- 可点击顺时针旋转 90° 的线材和灯泡。
- 基础线材：直线、弯线、T 形、十字形；灯泡为单接口。
- 实时显示已通电的灯泡与线材。
- "新游戏"生成一局新棋盘；"重置"恢复本局初始打乱布局。
- 完成时显示胜利提示。

不包含计时、计步、关卡存档、音效、动画、排行榜、自定义棋盘大小和移动端手势操作。

## 2. 技术选型

- **React + TypeScript + Vite**：棋盘格、控制区和胜利提示可拆为小组件；旋转后的状态和通电状态由 React 状态驱动；Vite 提供快速的本地开发与构建。
- **SVG + CSS**：使用 SVG 绘制电源、线材和灯泡，以统一绘制方向、通电颜色和格子边界，不依赖图片资源。
- **Vitest**：对不依赖界面的棋盘生成、旋转与连通性逻辑进行快速单元测试。
- **React Testing Library**：验证点击格子、重置与胜利提示等用户可见行为。
- **GitHub Actions**：push 到 main 后自动运行测试、构建并部署到 GitHub Pages。

不引入状态管理库或后端。游戏状态仅在浏览器内存中保存。

## 3. 游戏规则

### 3.1 格子与方向

棋盘坐标为 `row: 0..4`、`column: 0..4`。每个格子以 `N`、`E`、`S`、`W` 的集合保存当前接口方向。

- 电源格由生成器确定位置与接口方向，始终不可旋转。
- 灯泡格只有一个接口，点击后顺时针转向下一个方向。
- 直线、弯线、T 形格可旋转；十字形四个方向均连通，点击不改变其接口。
- 相邻两格只有在面对面的接口同时存在时才连接。例如左格有 `E` 且右格有 `W` 时，两格之间才导通。

### 3.2 通电与胜利

每次生成、重置或点击后，从电源格开始按相互匹配的接口广度优先遍历：

1. 电源格标记为通电。
2. 对每个已通电格，只访问位于棋盘内且接口与其相对的邻格。
3. 遍历到的格子为通电状态，其他格子为未通电状态。
4. 所有灯泡格均通电时，游戏胜利。

仅要求所有灯泡点亮；未通电的非灯泡线材不阻止胜利。

## 4. 可解棋盘生成

生成器必须先得到正确解，再打乱，不能直接随机摆放线材。每局恰好生成 5 个灯泡；灯泡均位于解状态生成树的叶子节点。

### 4.1 受约束的随机深度优先树

一次候选树按下列随机深度优先搜索生成：

1. 随机选择一个格子作为起点，标记为已访问并压入栈。
2. 读取栈顶格子，收集其上下左右中仍在棋盘内且尚未访问的邻格。
3. 若存在候选邻格，随机选取其中一个；为两个格子添加相对接口，将邻格标记为已访问并压入栈。
4. 若没有候选邻格，弹出栈顶，回退到上一格。
5. 栈清空时，全部 25 个格子已通过 24 条正交边组成一棵无环且全连通的生成树。

计算候选树中度数为 1 的节点数量。仅当该数量**恰为 5**时接受这棵树；否则丢弃并重新生成。生成器最多尝试 200 次。若仍未命中，则使用已验证的"梳形"覆盖树作为保底：一列纵向主干分别向其余四列延伸五条横向支路，其五个支路端点恰为叶子；再随机旋转或镜像该布局。保底布局同样覆盖 25 格且恰有 5 个叶子，因此生成过程始终能返回有效棋盘。

### 4.2 映射、打乱与可解性

1. 从已接受树的非叶子节点中随机选择一个作为电源，电源保留树中的所有接口且不可旋转。
2. 将恰好 5 个叶子节点映射为灯泡。
3. 其余非电源节点按连接数映射为线材：度数 2 且接口相对为直线，度数 2 且接口相邻为弯线，度数 3 为 T 形，度数 4 为十字形。
4. 深拷贝解状态，并将每个非电源格随机旋转 0～3 次，得到初始状态。若初始状态已使所有灯泡点亮，重新打乱，避免开局即胜利。
5. 保存初始打乱状态供"重置"使用；"新游戏"重新执行整个流程。

生成树覆盖全盘，因此解状态下每个灯泡都与电源连通；随机旋转只隐藏解，不会改变其存在性。生成器接收可注入的随机数函数，以便测试使用固定序列复现棋盘。

## 5. 数据与组件设计

### 5.1 核心类型

```ts
type Direction = 'N' | 'E' | 'S' | 'W';
type TileKind = 'power' | 'bulb' | 'straight' | 'corner' | 'tee' | 'cross';

type Tile = {
  id: string;
  kind: TileKind;
  connections: Direction[];
  powered: boolean;
};

type Board = Tile[][];

type GameState = {
  board: Board;
  initialBoard: Board;
  won: boolean;
};
```

`powered` 是通过遍历派生并在每次状态变更后写入的显示状态；`initialBoard` 必须深拷贝，不能与当前棋盘共享格子对象。

### 5.2 模块划分

- `game/directions.ts`：方向、相反方向、顺时针旋转。
- `game/generator.ts`：生成树、解状态和打乱。
- `game/power.ts`：从电源计算通电格与胜利状态。
- `game/gameReducer.ts`：处理旋转、新游戏和重置，组合生成与通电计算。
- `components/Board.tsx`：渲染 5×5 棋盘。
- `components/Tile.tsx`：用 SVG 根据接口和通电状态绘制单格，电源禁用点击。
- `components/GameControls.tsx`：新游戏与重置按钮。
- `App.tsx`：组装状态、棋盘、控件和胜利提示。

每个可操作格使用 `button`，并具有类似"第 2 行第 3 列，弯线，未通电"的可访问名称；电源格为禁用按钮或无点击行为的静态元素。

## 6. 开发步骤（TDD）

以下步骤均遵循"先写失败测试，再写最少实现使其通过，再重构"的循环。

1. 初始化 Vite React TypeScript 项目，配置 Vitest 与 React Testing Library；先添加一个能执行的示例测试。
2. 为方向模块编写测试：四个方向的相反映射、单次顺时针旋转、连续四次旋转回到原方向；完成最小实现。
3. 为通电遍历编写测试：双向接口可导通、单向接口不可导通、边界不越界、电源能点亮多段路径；完成遍历实现。
4. 为胜利判定编写测试：全部灯泡可达时为胜利，任一灯泡不可达时不胜利；完成判定实现。
5. 为生成器编写固定随机源测试：棋盘为 5×5、恰有一个电源、恰有 5 个灯泡、非电源格类型与生成树度数匹配、解状态中所有格可从电源到达、灯泡均为树叶；测试随机重试与梳形树保底分支后，完成生成树和类型映射实现。
6. 为打乱逻辑编写测试：不改变电源接口、不改变每格接口数量、能保留正确解、输出不是开局胜利；完成打乱和重试实现。
7. 为 reducer 编写测试：点击可旋转格只旋转该格并重算通电状态；点击电源无变化；重置恢复初始状态；新游戏替换棋盘；完成 reducer。
8. 先写组件交互测试：棋盘显示 25 格，点击一格改变其 SVG/可访问状态，点击重置恢复，构造已解棋盘时显示胜利提示；再实现组件。
9. 使用 SVG 和 CSS 完成视觉样式：未通电为低对比色，已通电线材与灯泡为高亮色；检查键盘操作与窄屏下的棋盘可用性。
10. 执行完整测试、类型检查和生产构建；启动本地服务完成手工验收。

## 7. 验收标准

### 自动化验收

- 单元测试覆盖方向旋转、双向连接规则、通电遍历、胜利判定、生成树和打乱约束。
- 组件测试覆盖旋转交互、不可旋转电源、新游戏、重置和胜利提示。
- 测试、TypeScript 类型检查和生产构建均成功。

### 手工验收

1. 打开页面，确认显示 5×5 共 25 个格子、一个电源和 5 个灯泡。
2. 点击线材或灯泡，确认其接口每次顺时针旋转 90°，通电显示同步变化。
3. 点击电源，确认其接口不发生变化。
4. 通过旋转将所有灯泡连至电源，确认全部灯泡高亮且显示胜利提示。
5. 点击"重置"，确认棋盘精确恢复为本局初始打乱布局，胜利提示消失。
6. 点击"新游戏"，确认生成了新的可交互棋盘；重复操作可完成该局。
7. 使用键盘 Tab 聚焦并按 Enter/Space 操作格子与控件，确认流程可完成。

## 8. 完成定义

实现仅在上述范围内，且通过全部自动化验收和手工验收后，首版核心功能完成。后续如需加入难度、计时或存档，应在不改变本规范中连通性判定的前提下单独扩展。
